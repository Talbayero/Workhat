NEXT_PUBLIC_APP_NAME=Work Hat CRM
NEXT_PUBLIC_APP_URL=https://work-hat.com

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Use Supabase Secret key (sb_secret_...) or legacy service_role JWT.
SUPABASE_SERVICE_ROLE_KEY=

# Work Hat Email Connector
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Use `openssl rand -base64 32` or any high-entropy 32+ character secret.
EMAIL_TOKEN_ENCRYPTION_KEY=
# Pub/Sub topic format: projects/{project-id}/topics/{topic-name}
GOOGLE_PUBSUB_TOPIC=
# Optional shared secret appended to the Pub/Sub push endpoint as ?token=...
GMAIL_PUSH_TOKEN=
# Used by Vercel Cron Authorization: Bearer ${CRON_SECRET}
CRON_SECRET=

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Email provider
POSTMARK_SERVER_TOKEN=
POSTMARK_INBOUND_TOKEN=
RESEND_API_KEY=

# Billing
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_PRO_ANNUAL=
STRIPE_PRICE_SCALE_MONTHLY=
STRIPE_PRICE_SCALE_ANNUAL=

# Security gateway
SECURITY_IP_BLACKLIST=
SECURITY_DYNAMIC_BLACKLIST_TTL_MS=
TURNSTILE_SECRET_KEY=

# Upstash Redis (required for API rate limiting)
# Create a database at https://console.upstash.com and copy the REST credentials.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Rate limiting (optional — safe to leave unset in local dev)
# Prefix for all Redis rate-limit keys. Change this if you share a Redis instance.
SECURITY_RATE_LIMIT_KEY_PREFIX=workhat:rate:v1
# Set to "true" to allow requests through when Redis is unreachable (default: false in production).
SECURITY_RATE_LIMIT_FAIL_OPEN=
